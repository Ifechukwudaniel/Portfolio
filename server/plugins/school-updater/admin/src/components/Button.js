import styled, { css } from "styled-components";
import { Button as PluginButton } from "@buffetjs/core";

const Button = styled(PluginButton)`
  height: 26px;
  margin: 0;
  padding: 0 15px;
  line-height: initial;
  font-size: 13px;
  font-weight: 500;
  margin-top: 30px;
  margin-left: 45px;
  font-weight: 400;
`;

export default Button;
